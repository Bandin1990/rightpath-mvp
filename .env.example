# Server-side only. Do not rename these to NEXT_PUBLIC_*.
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_REPLACE_ME

# Admin users authenticate through Supabase Auth. Set their authorization role
# in raw_app_meta_data (not user_metadata) to editor, reviewer, publisher, or admin.
